/.bundle/
/vendor/bundle
/pkg/
/tmp/
/log/*.log
/test/dummy/log/*.log
/test/dummy/tmp/
/test/dummy/db/*.sqlite3
Gemfile.lock
gemfiles/*.lock
.DS_Store
*.gem

# Full-resolution demo recording — too large for GitHub (>100MB).
# The committed GIF and the 640px MP4 are the shareable versions.
/i18n-proofreading-demo.mp4
